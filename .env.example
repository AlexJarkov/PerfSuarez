# Copia este archivo a .env y ajusta los valores.
# BASE_URL del ERP que provee el JSON del catálogo (sin slash final).
# Vacío = usar perfumes.json estático (fallback).
BASE_URL=
DISABLE_SHELL_REDIRECT=false
